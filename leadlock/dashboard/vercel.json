{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "https://api.leadlock.org/api/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
